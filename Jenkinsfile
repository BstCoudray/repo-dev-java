pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                //git url: 'https://github.com/bstcoudray/repo-dev-java', branch: 'main'
                checkout scm
            }
        }
    }
}
